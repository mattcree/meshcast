---
name: Bug report
about: Something doesn't work
labels: bug
---

**What happened / what you expected**


**Steps to reproduce**
1.
2.

**Environment**
- Meshcast version (`meshcast --version`):
- Role: bot / streamer / viewer
- OS + desktop (e.g. Fedora 43 GNOME Wayland, Windows 11, macOS 15):
- How installed: install.sh / install.ps1 / release archive / built from source
- Bot: deploy-bot.sh (system/user) / Docker / binary

**Logs**
<!-- Daemon: run `meshcast daemon` in a terminal (RUST_LOG=debug for more). Bot: journalctl -u meshcast-bot. Remove anything secret. -->
```
```
