## What / why


## Checklist
- [ ] `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` pass
- [ ] Scripts: `shellcheck scripts/*.sh` (if touched)
- [ ] `CHANGELOG.md` updated (user-visible changes)
- [ ] Protocol / config / install layout changes reflected in `docs/DESIGN.md` and compatible with the previous release
- [ ] Tested manually: <!-- what you ran, on what OS -->
